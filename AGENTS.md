# AGENTS.md

## 适用范围
本规则覆盖仓库根目录及其所有子目录。

## 强制规则
1. 任何功能、行为、依赖、脚本、架构改动，必须同步更新 `AI_PROJECT_CONTEXT.md`。
2. 面向用户或开发者可感知改动，必须写入 `CHANGELOG.md` 的 `Unreleased`。
3. PR 描述中需注明本次是否更新了 `AI_PROJECT_CONTEXT.md` 与 `CHANGELOG.md`；若未更新需说明理由。
